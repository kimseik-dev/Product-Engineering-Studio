{
  "name": "backend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "db:studio": "npx prisma studio",
    "db:push": "npx prisma db push",
    "db:generate": "npx prisma generate"
  },
  "devDependencies": {
    "prisma": "^7.5.0"
  },
  "dependencies": {
    "@prisma/client": "^7.5.0",
    "pg": "^8.20.0"
  }
}
